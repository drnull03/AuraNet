
image:
  repository: auranet/bootstrap
  tag: v2
  pullPolicy: Never

naiveConf: |
  1.retail-dashboard -> customer-api


# this moved to the engine itself and can be ignored 
settingsConf: |
  AI_Learning=true
  warm_start=false
  max_epochs=50