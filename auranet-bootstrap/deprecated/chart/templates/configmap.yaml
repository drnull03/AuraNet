apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "auranet-bootstrap.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
data:
  naive.conf: |
{{ .Values.naiveConf | indent 4 }}
  settings.conf: |
    {{ .Values.settingsConf | nindent 4 }}