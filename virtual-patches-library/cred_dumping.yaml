apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: auranet-prevent-ptrace
  namespace: default
spec:
  kprobes:
  - call: "sys_ptrace"
    syscall: true
    selectors:
    - matchActions:
      - action: Sigkill
