apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: auranet-prevent-cryptominers
  namespace: default
spec:
  kprobes:
  - call: "sys_execve"
    syscall: true
    selectors:
    - matchArgs:
      - index: 0
        operator: "In"
        values:
        - "/usr/bin/xmrig"
        - "/usr/local/bin/cgminer"
      matchActions:
      - action: Sigkill
