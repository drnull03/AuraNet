apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: auranet-prevent-namespace-escape
  namespace: default
spec:
  kprobes:
  - call: "sys_unshare"
    syscall: true
    selectors:
    - matchActions:
      - action: Sigkill
