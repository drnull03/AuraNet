kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4 # Kind API version
nodes:
  - role: control-plane            # One control-plane node
  - role: worker                   # First worker node
  - role: worker                   # Second worker node
networking:
  disableDefaultCNI: true          # Disables kindnet prior to Cilium install
