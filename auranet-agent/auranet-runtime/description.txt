AraNet Runtime: eBPF Agent Architecture

The AuraNet Runtime is the host-level security layer. While the AI Autoencoder monitors network packets between pods, the Runtime uses eBPF  to monitor system calls inside the containers.

This component is deliberately designed to be a fast, rule-based tripwire. It does not use Machine Learning. It looks for definitive, binary signs of compromise.


