apiVersion: v1
kind: ConfigMap
metadata:
  name: auranet-runtime-config
  namespace: auranet-namespace
  labels:
    app: auranet-runtime
data:
  # Helm's toJson function magically converts your YAML block into perfect JSON!
  threat-map.json: |
    {{ toJson .Values.runtime.threatMap }}