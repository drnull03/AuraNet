# === Neurosymbolic AI Threats ===
symbolic_null_byte_evasion=80
symbolic_uri_too_large=50
symbolic_banned_method=40
l7_payload_anomaly=45         # Requires at least 3 hits to quarantine
l7_body_anomaly=45            # Requires at least 3 hits to quarantine
network_behavior_anomaly=20

# === AuraNet Runtime Threats (eBPF) ===
reverse_shell_detected=100    # Immediate kill
container_escape_attempt=100  # Immediate kill
crypto_miner_execution=100    # Immediate kill
kernel_module_injection=100   # Immediate kill
k8s_token_theft=100           # Immediate kill
ssh_key_access=70
privilege_escalation=80
nc_execution=70
memory_dump_attempt=60
unauthorized_file_read=40
suspicious_binary_download=60
unauthorized_port_binding=60
nmap_scan_detected=40
unexpected_outbound_traffic=30

# === Fallback ===
unknown_anomaly=15
